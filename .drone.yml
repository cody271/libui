build:
        image: teaci/msys64
        shell: mingw64
        pull: true
        commands:
                - pacman -S --noconfirm --needed --noprogressbar cmake
                - mkdir build
                - cd build
                - cmake .. -G "Unix Makefiles"
                - make tester examples
