build:
        image: teaci/msys64
        shell: mingw64
        pull: true
        commands:
                - pacman -S --noconfirm --needed --noprogressbar cmake
                - mkdir build
                - cd build
                - cmake .. -G "MinGW Makefiles"
                - make tester examples
